## Basic terraform code to fetch "ami ID"

## We have taken this code from the terraform documentation, so we don't need to remember all it's syntax. 

data "aws_ami" "amiID" {                       ## data is a source that we use when we want the data outside of the terraform. ## "aws_ami" is the type of 
                                                 #  the resource which is predefined in terraform            
  most_recent = true                           ## "amiID" is the name of the resource.

  filter {                              
    name   = "name"
    values = ["ubuntu/images/hvm-ssd/ubuntu-jammy-22.04-amd64-server-*"]            ## These are the arguments that we passed inside the curly braces. 
  }                                                                                  #  So [most_recent, 2 filters, owners] these are just the arguments that 
                                                                                      # we've taken from terraform documentation to fetch the "amiID".
  filter {
    name   = "virtualization-type"
    values = ["hvm"]
  }

  owners = ["099720109477"]
}

output "instance_id" {                                                         ## "output" in terraform is like 'print' in python, output will print the the  
  description = "AMI ID of Ubuntu instance"                                     #   'value' of 'data.aws_ami.amiID.ID' in the output console.
  value       = data.aws_ami.amiID.id      
}




## terraform init --  If you don't have a terraform setup, this command will initialize the terraform for you.
## terraform validate -- this command will check if the code's syntax is correct or it needs a fix
## terraform apply -- this command to run the code